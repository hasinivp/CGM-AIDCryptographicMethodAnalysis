import time
import statistics
import tracemalloc
import csv

class Benchmark:
    def __init__(self):
        self.encrypt_times = []
        self.decrypt_times = []

    def encrypt_latency(self, func, *args, **kwargs):
        start = time.perf_counter()
        ciphertext = func(*args, **kwargs)
        end = time.perf_counter()
        self.encrypt_times.append(start-end)
        return ciphertext
    
    def decrypt_latency(self, func, *args, **kwargs):
        start = time.perf_counter()
        ciphertext = func(*args, **kwargs)
        end = time.perf_counter()
        self.decrypt_times.append(start-end)
        return ciphertext
    
    def encrypt_statstics(self):
        mean_val = statistics.mean(self.encrypt_times)
        median_val = statistics.median(self.encrypt_times)
        df = pd.DataFrame({ #generated by Gemini
            'Raw_Data': self.encrypt_times,
            'Mean': [mean_val] * len(self.encrypt_times),
            'Median': [median_val] * len(self.encrypt_times),
    
        })
        df.csv('encrypt_latency_data.csv', index=False)

     def decrypt_statstics(self):
        mean_val = statistics.mean(self.decrypt_times)
        median_val = statistics.median(self.decrypt_times)
        df = pd.DataFrame({ #generated by Gemini
            'Raw_Data': self.decrypt_times,
            'Mean': [mean_val] * len(self.decrypt_times),
            'Median': [median_val] * len(self.decrypt_times),
    
        })
        df.csv('decrypt_latency_data.csv', index=False)
